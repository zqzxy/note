##数组
	  数组-------------->引用类型  object--------->如何去判断一个数据的类型  typeof
            1. 数组：就是使用一个变量将一组数据保存起来，便于后期使用
            2. 作用
                *   保存数据，方便使用
                *   便于后期维护开发
            3.  数组的创建
                *   var arr=[]
                    var arr=[1,2,3]  var  arr=["1","2","你好","img/xx.png"]  var arr=[1,"aafsdfsa"] var arr=[[1,2,3],1,"safsa"]
            var city=[["浦东新区","嘉定"]，["尧都区"]]
                *   使用对象来创建一个数组
                        var arr=new Array()
                            var arr=new Array(1,2,3)
            4.  数组的长度
                arr.length
            5.  数组的下标(下标是从0开始的)------------》通过数组下标可以得知数组中的每一个数
                arr[0]  arr[1]  arr[2]
            6.  数组的遍历
                *   使用for循环
                    for(var i=0;i<arr.length;i++){
                        console.log(arr[i])
                    }
                *   使用for  in 来循环
                     for(var i in  arr){
                     //        console.log(i);
                     console.log(arr[i])
                     }
         7. "" 和 ' ' 的区别
                *   单独使用无区别的
                *   混合使用时，最外层使用""  里层 ‘’（字符串拼接的时候除外）
           8.   任务
                 1.  一组数中取出任意一个数
                 2.  二维数组中取出任意一个数
                 3.  找出数组中的最大值，并在控制台输出
                 4.  写一个函数 它的功能是返回一个二维数组中最长的小数组
                 5.  既有数字还有数组的话，判断里面数据的类型
                 var arr=[2,4,56,[2,3,4],78,5,[4,5,5],6,"asdkakda"];
                 6. 删除空值
                 var arr=[1,5,,4,6,7];var newarr=[];var n=0;
                    for(var i=0;i<arr.length;i++){

                    }
                 7 去重
                 var arr=[1,2,5,2,2,4,1]
                 8. 排序(冒泡排序法)
                 var arr=[7,5,6,9,1,2,3,4];
                 9. 既能判断一维数组又能判断二维数组
                 var arr3=[[1,2,5,6],4,5[4,8,9,44],[4,2,3,5,7]];
       
            9.	什么是二维或是多维数组
	            二维
					var arr=[1,2,[3,4]]
				多维(二维以上我们称之为多维数组)
					var arr=[1,2,[3,4,[5,6]]]
				使用length来区别几维数组||使用数据类型来区别